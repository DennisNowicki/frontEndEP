export class Components {
  constructor(
    public id: void,
    public name: string,
    public src: string,
    public fee: number,
    public minHours: number,
    public maxHours: number,
    public needed: boolean
  ) {}
}
